@using RunAndHikeStore.Web.ViewModels.Product
@model ProductViewModel


<div class="card">
    <div class="row">
        <div class="col-md-4">
            <img src=@Model.ImageUrl class="card-img-top img-thumbnail" alt="Product-photo" />
        </div>
        <div class="col-md-8">
            <div class="card-body">
                <h4 class="text-center">@Model.Brand</h4>
                <h5 class="text-center">@Model.Name</h5>
                <p class="fw-bold">Description:</p>
                <p>@Model.Description</p>
                <hr />
                <table class="table table-borderless">
                    <thead>
                        <tr>
                            <th scope="col">Product Number</th>
                            <th scope="col">Color</th>
                            <th scope="col">Gender</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>@Model.ProductNumber</td>
                            <td>@Model.Color</td>
                            <td>@Model.Gender</td>
                        </tr>
                    </tbody>
                    <tr>
                        <td>
                        </td>
                </table>
                <div class="mb-2">
                    <form>
                        <div class="d-flex justify-content-between">
                            <label asp-for="Size"><b>Size</b></label>
                            <select class="col-md-3" asp-for="SizeId">
                                @foreach (var size in Model.Sizes)
                                {
                                    <option value="@size.SizeId">@size.SizeName</option>
                                }
                            </select>
                            <span asp-validation-for="SizeId" class="text-danger"></span>
                            <label asp-for="UnitPrice"><b>Price</b></label>
                            <p class="mr-2">@Model.UnitPrice.ToString() EUR</p>
                        </div>
                        <div class="d-flex justify-content-end mt-2">
                            <input type="hidden" name="productId" value="@Model.Id" />
                            <input asp-controller="ShoppingCart" asp-action="AddToCart" asp-route-productId=@Model.Id asp-route-sizeId=@Model.SizeId class="btn-lg btn-success col-md-4 mt-3" type="submit" value="Add to cart" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}