@using RunAndHikeStore.Web.ViewModels.Brand
@model IEnumerable<BrandViewModel>

<h2 class="text-center">@ViewBag.Title</h2>
<hr />
<div class="d-grid gap-2 mb-4">
    <a asp-controller="Brand" asp-action="Add" class="btn btn-success">Add New Brand</a>
</div>

<table class="table table-hover">
    <thead>
        <tr class="table-dark text-center">
            <th scope="col">Brand</th>
            <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var brand in Model)
        {
            <tr class="table-dark text-center">
                <td>@brand.Name</td>
                <td>
                    <a asp-controller="Brand" asp-action="Edit" asp-route-id="@brand.Id" class="btn btn-warning mr-2">Edit</a>
                    <a asp-controller="Brand" asp-action="Delete" asp-route-id="@brand.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}